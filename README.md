# Tenerife Guide

Static SPA for `jazumaliving.com`, property `Jazuma Paradise`.

## Local

```bash
npm install
npm run build:data
npm run dev
```

## Gates

- No public secrets in generated JSON.
- `npm run lint`
- `npm run build`
- Refresh smoke test on `/paradise`, `/paradise/guide`, `/paradise/kontakty`.
